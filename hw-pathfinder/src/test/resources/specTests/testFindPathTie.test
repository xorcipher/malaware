# Find a path with Dijkstra's algorithm when
# the nodes entered have two paths between them
# with the same cost. This should result
# in a path with the least number of hops.

CreateGraph simpleGraph

AddNode simpleGraph A
AddNode simpleGraph B
AddNode simpleGraph C

AddEdge simpleGraph A C 5.0
AddEdge simpleGraph A B 2.5
AddEdge simpleGraph B C 2.5

FindPath simpleGraph A C